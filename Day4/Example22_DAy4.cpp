#include <complex>
#include <iostream>
#include <vector>
#include <iomanip>

/*int main() {

	//std::cout << "Hi";
	int a1=0; // undefined value
	// int a2 = 10.5; // Error Double to Int
	// int a3(10.4); // Error Double to Int
	int a4 = int(); // initializes with zero
	int a5{}; // initializes with zero
	//int a6 = { 10.6 }; //  It Gives Narrowing ERROR
	//int a7{ 10.4 }; // ERROR
	int a8 = {}; // initializes with zero
	int values[] = { 1, 2, 3 }; // initialization of aggregates
	std::complex<double> c(10.4, 20.5); // initialization of classes
	std::complex<double> c1{ 10.4, 20.5 }; 
	std::vector<int> age; 
	std::vector<int> age1{ 10, 20 }; 

	auto v1 = 12345; // v1 is int
	auto v2 = 'c'; // v2 is char
	auto v3={ 12345 }; // v3 is a list of a int
	auto v4 = { 'c' };

	for (auto i : v3) {
		std::cout << i;
	}

	int a=0;
	std::cout << "Value of a: " << a << std::endl;
	std::cout << std::setprecision(6) << 3.14159265358979 <<std::endl;


	//int x = 5;
	//auto y = x;              // y is inferred as an int (copy of x)
//	decltype(x) z;           // z is deduced as int


	int x = 5;
	decltype(auto) ref1 = x;     // ref1 is deduced as int&
	decltype(auto) ref2 = 3.14;  // ref2 is deduced as double&&

}*/